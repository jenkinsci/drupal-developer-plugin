<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${%Name}" field="name">
    <f:textbox />
  </f:entry>
  <!-- TODO path to drush executable itself ? instead of directory -->
  <f:entry title="Path to drush executables directory" field="home">
    <f:textbox />
  </f:entry>
</j:jelly>